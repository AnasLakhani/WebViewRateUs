<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.WebviewApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@android:color/holo_blue_bright</item>
        <item name="fontFamily">@font/poppins_regular</item>
    </style>

    <style name="Theme.WebviewApplication" parent="Base.Theme.WebviewApplication" />

    <style name="Theme.WebviewApplication.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.WebviewApplication.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.WebviewApplication.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>